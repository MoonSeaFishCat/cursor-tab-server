# 实施任务计划：带管理控制台的受鉴权 Cursor 代理

## 目标

将现有单文件 Go Cursor 代理重构为带 SQLite、调用 API Key、管理员会话、审计日志、限流和嵌入式 React + shadcn/ui 管理控制台的单镜像服务。

## 已确认的决策

- 管理与代理接口都必须鉴权。
- 管理端支持公网访问，但必须由 HTTPS 反向代理暴露；应用默认监听 `127.0.0.1:8041`。
- SQLite 保存调用密钥、会话和审计日志，日志保留 30 天。
- 管理员使用 8 小时 HttpOnly 安全 Cookie 会话；管理员密钥仅通过登录请求提交，不存储于浏览器。
- 前端使用 React、Vite 与 shadcn/ui，构建产物通过 Go `embed` 内嵌。
- UI 使用固定侧边栏，采用中性、留白较多的视觉风格。

## 阶段

| 阶段 | 状态 | 交付物 |
| --- | --- | --- |
| 1. 设计和计划 | 已完成 | 更新的设计规范和详细实施计划 |
| 2. Go 基础设施 | 已完成 | 配置、SQLite 存储、密钥与会话、审计和限流 |
| 3. 受保护的 HTTP API 与代理 | 已完成 | 管理 API、会话、代理鉴权和日志 |
| 4. React 管理控制台 | 已完成 | 登录、概览、密钥、日志、状态及嵌入构建 |
| 6. 账户密码验证码登录改造 | 已完成 | 环境变量管理员账号、一次性图形验证码登录、前端页面与根目录启动入口 |

## 当前阶段

阶段 6 已完成：管理员登录使用 `ADMIN_USERNAME`、`ADMIN_PASSWORD` 与一次性图形验证码；启动入口已移动至项目根目录 `main.go`。

## 下一步

部署时设置管理员账号环境变量，并在 HTTPS 反向代理后启动服务。

## 错误记录

| 时间 | 问题 | 处理 |
| --- | --- | --- |
| 2026-08-26 | 视觉设计辅助工具在 WSL Bash 上缺少 `/bin/bash` | 改用 Git for Windows Bash。 |
| 2026-08-26 | Git Bash 未加载 Unix 工具路径 | 改用 `bash -lc` 启动，设计辅助工具成功运行。 |
